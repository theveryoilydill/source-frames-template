name: Check and Lint

on:
  pull_request:
  push:
    branches:
      - main
      - develop

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      # Default ref: for pull_request events this is the merge commit GitHub
      # would actually ship, not just the PR head.
      - name: Checkout
        if: github.event_name != 'workflow_run'
        uses: actions/checkout@v6

      - name: Setup PNPM
        uses: pnpm/action-setup@v6
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 26
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Oxlint
        id: lint
        run: pnpm lint
        continue-on-error: true

      - name: Format check
        id: fmt
        run: pnpm fmt:check
        continue-on-error: true

      - name: Type Check
        id: typecheck
        run: pnpm typecheck
        continue-on-error: true

      - name: React Router Build
        id: build
        run: pnpm run build
        continue-on-error: true

      - name: Fail if any checks failed
        if: |
          always() &&
          (
            steps.lint.outcome == 'failure' ||
            steps.fmt.outcome == 'failure' ||
            steps.typecheck.outcome == 'failure' ||
            steps.build.outcome == 'failure'
          )
        run: |
          echo "One or more checks failed."
          exit 1
